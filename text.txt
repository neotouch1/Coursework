В коде выполняется дополнение изображения (padding) до размеров, кратных размеру блока (block_size). Это делается для подготовки к сжатию через JPEG, поскольку алгоритм JPEG работает с изображением, разбивая его на блоки фиксированного размера — обычно 8×8 пикселей.

🔎 Зачем это нужно в JPEG?
Алгоритм JPEG основан на дискретном косинусном преобразовании (DCT), которое применяется к блокам изображения размером 8×8 пикселей.
Если ширина или высота изображения не кратна 8, то последние блоки будут неполными, что вызовет проблемы при сжатии и декодировании.
Поэтому изображение дополняется (обычно нулями или цветом фона), чтобы его размеры стали кратными 8.
🧠 Как это работает в коде
Определение недостатка в размерах
Если размер изображения (h и w) не кратен block_size, вычисляется количество пикселей, которых не хватает





K.1 Таблицы квантования для компонентов яркости и цветности
Два примера таблиц квантования приведены в таблицах K.1 и K.2. Они основаны на психовизуальном пороговом значении и
выведены эмпирически с использованием яркости и цветности и горизонтальной подвыборки 2:1. Эти таблицы приведены только в качестве примеров и не обязательно подходят для какого-либо конкретного приложения. Эти значения квантования ​​были использованы
с хорошими результатами на 8-битных на выборку изображениях яркости и цветности формата, показанного на рисунке 13. Обратите внимание, что
эти значения квантования ​​подходят для нормализации DCT, определенной в A.3.3.
Если эти значения квантования разделить на 2, то полученное восстановленное изображение обычно почти неотличимо от
исходного изображения.


